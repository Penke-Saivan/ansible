---
- name: loops
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: Print names through loops 
    ansible.builtin.dnf:
      name: "{{item}}"
      state: latest
    loop:
    - nginx
    - mysql
    - zip
